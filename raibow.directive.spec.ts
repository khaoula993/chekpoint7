import { RaibowDirective } from './raibow.directive';

describe('RaibowDirective', () => {
  it('should create an instance', () => {
    const directive = new RaibowDirective();
    expect(directive).toBeTruthy();
  });
});
